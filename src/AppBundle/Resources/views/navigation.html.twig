{% extends 'base.html.twig' %}

{% block navigation %}


{% if global_access %}
	<a href="{{ path('left') }}" class="textwhite">Sign out</a>

{% else %}

   <div class="logheader">
   <a href="{{ path('auth') }}" class="col-xs-1 col-xs-offset-8 col-md-1 col-md-offset-9 blockintro">Sign in</a> 
   <a href="{{ path('join') }}" class="row col-xs-1 col-xs-offset-1 col-md-1 col-md-offset-0 blockintro">Sign up</a>
   </div>

{% endif %}

{% endblock %}